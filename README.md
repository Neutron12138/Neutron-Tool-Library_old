# Neutron-Tool-Library
一个简单的C++工具库

## 改进

删除了string/下的许多东西

之后所有字符串都必须为宽字符

## 接下来

接下来完成节点（Node）

## 其他

就效果而言String/StringUtils.cpp比之前版本可读性增加了许多

之后会把所有文件都改成这样

### 格式:

（模板）template<typename T>

（返回值）bool

（函数名）is_equal(

（参数）const T& a,

const T& b)

（函数体）{...}
